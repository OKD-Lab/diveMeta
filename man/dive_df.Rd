% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dive_df.R
\name{dive_df}
\alias{dive_df}
\title{DiVE from a data.frame}
\usage{
dive_df(
  data,
  cols = list(med_g1 = "med_g1", n_g1 = "n_g1", med_g2 = "med_g2", n_g2 = "n_g2"),
  direction = c("g1_minus_g2", "g2_minus_g1"),
  ci_type = c("t", "normal")
)
}
\arguments{
\item{data}{data.frame with columns for medians and sample sizes}

\item{cols}{named list mapping column names}

\item{direction}{character, \code{"g1_minus_g2"} (default) or \code{"g2_minus_g1"}}

\item{ci_type}{character, \code{"t"} (default) or \code{"normal"}}
}
\value{
An object of class \code{"dive"} (see \code{\link{dive}}).
}
\description{
Wrapper that maps columns in \code{data} to \code{dive()} inputs.
}
\examples{
dat <- read_example("Langhorne_ESD_all.csv")

# Build central tendencies (median preferred; mean as proxy)
dat_ct <- transform(
  dat,
  ct_g1 = ifelse(!is.na(median_g1), median_g1, mean_g1),
  ct_g2 = ifelse(!is.na(median_g2), median_g2, mean_g2)
)

# Drop studies with no usable central tendency in either group
dat_ct <- subset(dat_ct, !is.na(ct_g1) & !is.na(ct_g2))

fit <- dive_df(
  dat_ct,
  cols = list(med_g1 = "ct_g1", n_g1 = "n_g1",
              med_g2 = "ct_g2", n_g2 = "n_g2"),
  direction = "g1_minus_g2",
  ci_type   = "t"
)
print(fit)
}
