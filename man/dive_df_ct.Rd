% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dive_df_ct.R
\name{dive_df_ct}
\alias{dive_df_ct}
\title{DiVE with automatic central tendencies (median first, else mean)}
\usage{
dive_df_ct(
  data,
  direction = c("g1_minus_g2", "g2_minus_g1"),
  ci_type = c("t", "normal"),
  policy = c("median_first", "median_only", "mean_only")
)
}
\arguments{
\item{data}{A data.frame containing at least \code{n_g1}, \code{n_g2} and
one of \code{median_g1}/\code{mean_g1}, \code{median_g2}/\code{mean_g2}.}

\item{direction}{Character, passed to \code{\link[=dive_df]{dive_df()}}.}

\item{ci_type}{Character, passed to \code{\link[=dive_df]{dive_df()}}.}

\item{policy}{One of \code{c("median_first","median_only","mean_only")}.
\code{"median_first"} uses median when available, otherwise mean.}
}
\value{
An object of class \code{"dive"} (see \code{\link[=dive]{dive()}}).
}
\description{
Convenience wrapper that builds per-study central tendencies and then calls
\code{\link[=dive_df]{dive_df()}}. If a median is available it is used; otherwise the mean is used
as a proxy under approximate symmetry.
}
\examples{
# dat <- read_example("oyelade_sdnn_org.csv")
# fit <- dive_df_ct(dat, direction="g1_minus_g2", ci_type="t")
# print(fit)
}
