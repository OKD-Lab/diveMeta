% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dive.R, R/dive_df.R
\name{dive}
\alias{dive}
\alias{print.dive}
\alias{summary.dive}
\title{DiVE meta-analysis using medians}
\usage{
dive(
  med_g1,
  n_g1,
  med_g2,
  n_g2,
  direction = c("g1_minus_g2", "g2_minus_g1"),
  ci_type = c("t", "normal")
)

\method{print}{dive}(x, ...)

\method{summary}{dive}(object, ...)
}
\arguments{
\item{med_g1}{numeric, group 1 medians (length K)}

\item{n_g1}{integer, group 1 sample sizes (length K, >0)}

\item{med_g2}{numeric, group 2 medians (length K)}

\item{n_g2}{integer, group 2 sample sizes (length K, >0)}

\item{direction}{character, \code{"g1_minus_g2"} (default) or \code{"g2_minus_g1"}}

\item{ci_type}{character, \code{"t"} (default) or \code{"normal"}}

\item{x}{An object of class \code{dive}.}

\item{...}{Further arguments passed to or from other methods (unused).}

\item{object}{An object of class \code{dive}.}
}
\value{
An object of class \code{"dive"} with fields:
\code{estimate}, \code{se}, \code{ci_low}, \code{ci_high},
\code{var_hat}, \code{weights}, \code{wtilde}, and \code{diagnostics}.
}
\description{
Pooled between-group difference using sample-size (IVW-style) weights and
a direct estimator of the variance; no within-study variances are required.
}
\examples{
med_g1 <- c(30,28,26); n_g1 <- c(50,60,55)
med_g2 <- c(25,27,24); n_g2 <- c(50,65,50)
fit <- dive(med_g1, n_g1, med_g2, n_g2)
print(fit)
}
